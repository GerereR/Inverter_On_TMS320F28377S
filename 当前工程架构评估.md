# InverterBase 当前工程架构评估

## 1. 结论

当前工程的目录划分和功能拆分方向是合理的，已经明显优于老工程，适合作为逆变器控制的 bring-up（上电验证）框架。

但它目前还不能称为最终的实时控制架构。主要原因不是文件夹数量或命名，而是：

- 实际模块依赖存在交叉和环路；
- ISR、任务和硬件之间的所有权没有完全明确；
- 共享数据没有稳定快照机制；
- 启动状态机尚未覆盖真实的 Boost、母线和继电器流程；
- 20 kHz 快速路径还没有做最坏执行时间验证。

因此，当前阶段应把它看成“可以继续跑通功能的骨架”，而不是“已经可以长期扩展的产品架构”。

本文件只记录架构评估和后续方向，不替代具体控制算法的参数设计。

## 2. 当前目录和运行关系

```text
z0_PCtools      上位机 Python 工具
z1_cmd          Linker command 文件和内存布局
z2_driver       TI 官方底层驱动（只读参考）
z3_system       系统初始化、安全输出、公共工具
z4_scheduler    1 ms 协作式调度器
z5_task         状态、测量、通信、UI、MPPT 等任务
z6_bsp          ADC、DMA、eCAP、ePWM、GPIO、SCI、I2C 配置和 ISR
z7_data         常量、数据类型、运行时全局数据
z8_control      PLL、母线环、Boost 环、电流环、功率限幅、无功控制
```

当前主循环大致为：

```text
1 ms Timer ISR
    -> Scheduler flags
    -> Task_Measure / Task_State / Task_Grid
    -> Task_PowerLimit / Task_ReactiveCtrl / Task_DcCtrl
    -> Task_MPPT / Task_Comm / Task_Eeprom / Task_UI

20 kHz ADCA ISR
    -> 读取快速 ADC
    -> SOGI-PLL
    -> 电流环
    -> PWM 更新

DMA ISR
    -> 记录完成的 DMA block
    -> 交给 Task_Measure 计算 AVG、RMS 和功率

eCAP ISR
    -> 计算电网频率
    -> 产生过零事件
    -> 当前还会参与快速 DMA 边界切换
```

## 3. 已经做对的部分

1. 已经把硬件配置、任务、控制算法和运行数据分开，阅读和定位问题比老工程容易。
2. 快速电流控制放在 ADC ISR，慢速统计放在任务中，整体多速率思路正确。
3. ADC 原始结构按电网、母线、PV 和温度等信号命名，上层不必依赖具体 ADC 引脚。
4. DMA 已按快速、中速、慢速数据组拆分，后续扩展通道时有明确落点。
5. PLL、母线环、Boost 环、电流环、功率限幅和无功控制已经有独立入口。
6. TZ 硬件封波和软件安全输出同时存在，保护方向正确。
7. `MachineData` 统一保存原始值、瞬时真实值、AVG、RMS 和功率数据，可作为上层数据接口的基础。
8. 常量中的机型参数和测量校准参数已经开始与运行数据分离。

## 4. 必须优先处理的问题

### P0：启动流程存在逻辑死锁

`z5_task/task_state.c:113` 的 `State_IsReady_BUS()` 要求母线已经处于 `370 V ~ 430 V`。

但 `z5_task/task_dc_ctrl.c:16` 在系统进入 `NORMAL` 之前直接返回，因此 Boost 和母线环不会在 `CHECK` 阶段建立母线。

结果是：

```text
PV 有电、母线没电
    -> 进入 CHECK
    -> 等待母线 370~430 V
    -> DC 控制因为尚未 NORMAL 而不运行
    -> 永远无法进入 NORMAL
```

必须明确以下两种方案中的一种：

- 增加 `PRECHARGE` 或 `BOOST_START` 阶段，在允许条件下先建立母线；
- 明确硬件要求母线必须由外部电源预充，软件只负责检查。

### P0：状态机的逻辑状态和物理硬件状态不一致

`z5_task/task_state.c:335` 将 `relayReady` 固定为 0，但后面的 `CHECK_PREPARE` 没有检查它就允许进入 `NORMAL`。

同时，当前流程没有清晰体现：

- 哪一步打开 Boost 栅极驱动；
- 哪一步打开逆变器栅极驱动；
- 哪一步闭合并网继电器；
- 哪一步验证继电器反馈；
- `NORMAL` 是否表示“控制器已准备好”，还是“功率级已经实际并网”。

在继续实现状态机前，必须先定义每个状态的硬件前提和 Entry/Run/Exit 动作。

### P0：DMA 和 eCAP 边界处理存在竞态

`z6_bsp/dma.c:276` 在 eCAP ISR 中停止 DMA、等待 DMA 停止、切换缓冲区并重新配置；`z6_bsp/dma.c:304` 的 DMA 完成 ISR 也会切换同一组活动缓冲区状态。

可能出现：

- eCAP 边界和 DMA 满块同时到达时重复切换；
- 当前 block 被截断或覆盖；
- eCAP ISR 被忙等拉长，影响 ADC 和其他中断；
- 过零点附近出现数据块长度不稳定。

另外，`DMA_StartNextBlock()`、`DMA_HaltFastBlock()` 和 `DMA_RestartFastBlock()` 的运行时控制寄存器写入没有明显包在 `EALLOW/EDIS` 中，而官方 DMA 驱动对同类操作使用了 `EALLOW`。这部分必须先验证，否则 DMA 重启可能根本没有生效。

建议的方向是：使用连续 ping-pong 或 DMA 队列，eCAP 只发布边界事件和计数，不在高优先级 ISR 中忙等和重配 DMA。

### P1：20 kHz ADC ISR 尚未做实时性证明

`z6_bsp/adc.c:187` 的 ISR 每次会调用 `Ctrl_FastRun()`；`z8_control/current_ctrl.c:222` 内含：

- SOGI-PLL；
- 浮点 PI；
- 锁相判断；
- 峰值判断；
- TMU 正弦计算；
- 浮点除法；
- PWM 更新。

20 kHz 的周期只有 50 us。当前 Debug 配置仍是 `-O0`（`.cproject:39`），并且程序默认从 Flash 执行，所以当前只能说明“代码能编译”，不能说明“每次都能按时完成”。

后续必须记录：

- ADC ISR 最大执行时间；
- ADCINT 溢出次数；
- Timer0 与 ADC ISR 的嵌套/延迟情况；
- DMA 和 eCAP ISR 对快速环的影响。

### P1：MachineData 没有一致性快照

`Task_Measure()` 在主循环中发布多个结构体（`z5_task/task_measure.c:252`），而 PLL 频率等字段在 ADC ISR 中更新（`z8_control/current_ctrl.c:235`）。

`volatile` 只能保证编译器不删除访问，不能保证一个包含多个 `float` 和 `Uint32` 的结构体一次性完成读写。UI、SCI、状态机可能读到混合帧。

另外，不同数据组的更新时间不同：

- 电网快速数据：通常约一个工频周期；
- PV DMA：约 8 ms；
- 隔离和温度：约 160 ms。

现在它们都放在同一个 `MachineData` 中，但没有 `seq`、时间戳、有效标志或数据年龄，使用者无法判断某个字段是不是旧数据。

建议保留当前结构体的可读性，同时增加：

- 分组序号；
- 时间戳或 age；
- valid 标志；
- 双缓冲或 sequence-lock 发布机制。

### P1：故障、PWM 和控制命令没有唯一拥有者

当前 ADC ISR、TZ ISR 和状态机都可能直接修改故障位。位域赋值通常是 read-modify-write，并发修改时可能丢失另一方刚置位的故障。

PWM 则同时由以下路径操作：

- ADC ISR 中的 `EPWM_SetInverterMode()`；
- `Task_DcCtrl()` 中的 Boost 更新；
- `System_EnterSafeOutput()`；
- TZ ISR。

这会产生安全关闭与下一次调制更新互相覆盖的可能性。

建议：

1. 每个故障定义唯一生产者，其他模块只上报事件；
2. 使用集中式 `Fault_Set()`、`Fault_Clear()` 和锁存策略；
3. 让一个输出管理模块成为 PWM 命令的唯一提交者；
4. 继续保留 TZ 作为不依赖软件调度的最终硬件关断。

### P1：调度标志会丢事件，阻塞任务会拖慢全系统

`z4_scheduler/scheduler.c:28` 使用 bit flag。多个过零或峰值事件在主循环来不及处理时会合并成一次。

这对“只关心最新值”的周期任务可以接受，但对以下事件不一定可以接受：

- 每个过零点的电网数据计算；
- 每个峰值的母线/Boost 更新；
- 必须逐块消费的 DMA 完成事件。

同时，SCI、OLED、I2C 和 EEPROM 仍有轮询等待。特别是 `SCI_SendByte()` 没有超时，通信任务可能长时间占用协作式主循环。

建议区分两种事件：

- latest-value：允许覆盖，只保留最新值；
- every-event：使用计数器、环形队列或 DMA block 队列。

### P2：文件夹分层正确，但实际依赖仍然交叉

当前依赖大致为：

```text
ADC/BSP -> Scheduler
ADC/BSP -> Control
Control -> BSP
System -> Control
Task -> BSP + Control + Data
```

例如 `adc.c` 直接调用 `Ctrl_FastRun()`，`dma.c` 直接完成应用层均值和平方和计算，`current_ctrl.c` 直接写 PWM。这使得更换 ADC、DMA 或 MCU 时会牵动控制算法和任务代码。

目标不是完全禁止 ISR 调用控制算法，而是把接口变成“输入快照 -> 控制结果”，让控制函数本身不依赖寄存器和具体芯片类型。

### P2：移植性和工程配置仍带有 F28377S 痕迹

控制层仍直接依赖：

- `F28x_Project.h`；
- `__sinpuf32()`、`__cospuf32()`；
- TI 寄存器结构体；
- `Uint16`、`Uint32` 和 linker section；
- `EALLOW/DINT/EINT`。

`.cproject` 还残留 `CLA_C`、CLA math library 和 `--cla_support=cla1` 配置（`.cproject:36`、`.cproject:73`、`.cproject:82`）。这不影响当前目录的概念分层，但会妨碍未来移植到没有 CLA 的 MCU。

## 5. 推荐的目标架构

```text
                 +----------------------+
ADC/eCAP/DMA -->| BSP ISR 壳            |
                 | 采样、清标志、发布事件 |
                 +----------+-----------+
                            |
                            v
                 +----------------------+
                 | Realtime Control     |
                 | PLL / 电流环 / 调制计算|
                 | 输入结构 -> 输出结构   |
                 +----------+-----------+
                            |
                            v
                 +----------------------+
                 | PWM Output Manager   |
                 | 唯一提交者 + TZ关断    |
                 +----------------------+

DMA block ------> Measurement Service -> MachineData snapshot
                                        |
                                        v
                         State / Limit / MPPT / UI / SCI / EEPROM tasks
```

建议遵守以下边界：

- BSP 负责寄存器配置、采样读取、中断清标志和硬件输出接口；
- 控制函数只处理明确的输入结构、内部状态和输出结构；
- 测量服务负责 DMA block 的消费、校准、AVG、RMS 和功率计算；
- 状态机是系统运行许可的唯一管理者；
- 故障模块是故障锁存和恢复的唯一管理者；
- PWM 输出管理器是软件 PWM 命令的唯一提交者；
- UI、SCI、EEPROM 只能读取已发布快照或提交命令，不能直接修改控制器内部历史量。

## 6. 建议的执行顺序

- [ ] 明确母线启动策略：外部预充，还是软件 Boost 预充。
- [ ] 定义 `WAIT/CHECK/NORMAL/FAULT/PERMANENT` 每个状态的硬件前提和动作。
- [ ] 修正并验证 DMA 运行时 `EALLOW`，消除 eCAP ISR 中的忙等重配。
- [ ] 对 ADC、eCAP、DMA、Timer0 ISR 做 WCET 和溢出统计。
- [ ] 建立 MachineData 分组快照、序号、时间戳和有效标志。
- [ ] 建立 Fault API，整理每个故障的生产者、清除者和恢复条件。
- [ ] 建立 PWM 命令拥有者，规定安全关闭和调制更新的优先级。
- [ ] 将调度事件区分为计数事件和 latest-value 事件。
- [ ] 逐步把 SCI、OLED、EEPROM 的长轮询操作改成有界或非阻塞服务。
- [ ] 清理 CLA 残留配置，集中平台相关定义。
- [ ] 在架构稳定后再实现完整功率限幅、无功控制和继电器自检。

## 7. 当前阶段可以保留的取舍

### 建议保留

- 多速率任务思想；
- ADC ISR 中的快速闭环；
- DMA 负责搬运、任务负责统计；
- 语义化的 ADC 数据结构；
- PLL、母线环、Boost 环、电流环的独立入口；
- TZ 硬件保护加软件安全输出；
- 机型参数和校准参数集中管理。

### 暂时不要继续扩大的部分

- 在未解决快照和所有权之前继续增加全局变量；
- 在 eCAP ISR 中继续加入复杂计算；
- 让任务直接修改控制器内部积分器；
- 让多个模块直接写同一个 PWM 或故障字；
- 在启动状态机未闭合前继续添加并网动作。

## 8. 待确认事项

这些问题会决定下一轮状态机和控制接口的具体实现：

1. 母线启动时是否允许软件 Boost 在 `NORMAL` 之前运行？
2. `NORMAL` 是“控制器允许运行”，还是“继电器已经闭合并网”？
3. 继电器是否有真实反馈输入？没有反馈时，软件是否只做时序控制？
4. 电网数据是否必须每个周期都处理，还是允许只保留最新完整周期？
5. DMA block 丢失时，哪些数据可以丢，哪些数据必须触发故障？
6. SRF-PLL 和 SOGI-PLL 未来是否需要同时运行，以便做客观 A/B 比较？

## 9. 评审备注

- 本轮只新增了这份文档，没有修改工程源代码。
- 当前工程最近一次构建成功，但 Debug 配置仍为 `-O0`，不能代替实时性测试。
- 工作区原有未提交修改保持不动。
