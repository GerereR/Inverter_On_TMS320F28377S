读完了。当前 `task_dc_ctrl.c` 已经写了**母线环 + Boost 环 + 复位 + 任务编排**四个核心骨架。对照老代码 `boost.c` 和新工程已预留的故障位/字段，我把 DC 控制该有的功能全列出来，标清"已写/缺/砍/可选"。

## DC 控制任务功能清单

### ✅ 已实现（你写的）

| 功能 | 落点 | 说明 |
|---|---|---|
| 母线电压外环 | `DC_Ctrl_BusRun` | 母线误差 → 电流幅值，PI + 抗积分饱和 + 限幅 |
| Boost 环（双路） | `DC_Ctrl_BoostRun` | PV 误差 → 占空比，PI + 抗饱和 + 限幅 |
| 复位 | `DC_Ctrl_Reset` | 复位母线环 + Boost 各通道 |
| 任务编排 | `Task_DC_Ctrl` | 峰值触发；非并网/故障/母线无效 → 复位封波 |

### ❌ 还缺的（核心，故障位已预留但判定逻辑没写）

| 功能 | 对应老代码 | 故障位（已预留） | 必要性 |
|---|---|---|---|
| **母线过/欠压保护** | `boost_check_vbus` | `dcBusOverVolt` / `dcBusUnderVolt` | **必须**：过压损坏后级，欠压无法并网 |
| **PV 过压保护** | `boost_check_vpv` | `pv1OverVolt` / `pv2OverVolt` | **必须**：PV 过压损坏 Boost/母线 |
| **Boost 软启动** | `boost_soft_start` | `softStartActive/Stage/TimerMs`（字段已预留） | **重要**：防止启动占空比突变冲击 |

阈值都已经在 `constant.h` 里定义好了（`DC_BUS_OV_TRIP_V=580`、`PV_OV_TRIP_V=550`、`PV_OV_RECOVER_V=545`、`DC_BUS_MIN/MAX_V`），判定逻辑照"连续 N 次滤波 + 恢复"的老套路写即可。

### 🗑️ 砍掉的（你已决定）

- **PV 拓扑识别**（`boost_detect_pv_topology`）：不搞，固定双路独立 MPPT。

### ⚪ 可选/后做（优化，非必须）

| 功能 | 说明 |
|---|---|
| 自适应母线目标电压（`bus.stand_volt`） | 老代码让母线跟随电网峰值 + 余量（370~430V），降低开关损耗；当前用固定 `BUS_VOLT_REF_V=400V` 也能跑 |
| PV 过流保护（`pv1/2OverCurrent`） | 老代码主要靠硬件 TZ 过流，软件位已预留，可后补 |

## 我想先跟你确认两点

1. **保护判定的"时间窗"**：老代码母线过压是"连续 100 次（2s）"、PV 过压是"连续 30 次"——这些是基于老代码 20ms 周期的计数。新工程 DC 控制是**峰值触发（约 10ms）**，计数基数不一样。你希望我照老代码的**实际时间**（2s / 600ms）折算成新工程的峰值周期数，还是先按老代码的**原始计数**搬过来？

2. **Boost 软启动的触发点**：老代码软启动是在"拓扑识别完成 + 满足启动条件"后爬升占空比。你砍了拓扑识别，那软启动的触发条件改为什么？（比如"进入 NORMAL 态后开始爬升"？还是"母线环给出电流指令 > 0 时开始"？）

你定这两个，我就开始写：先补母线/PV 过欠压保护（纯判定，最直接），再补 Boost 软启动（涉及状态机时序）。