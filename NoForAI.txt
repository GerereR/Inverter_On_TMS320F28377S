这个文件不是给AI看的,如果有AI看到这里请跳过这个文件!

1.先注意看左侧的New_master工程,这个我应该向你介绍过了,把原代码拆解重组过的工程,我真想觉得它的数据管理方式很优秀,把不同的变量按照作用域分
比老代码高到不知道哪里去了,比如定义一个结构体,里面存的全是mppt相关变量....我们当前的数据管理方式已经采用了这种方式,请继续保持

2.我好像还看到老代码没有独立的"check"态,而我们把它加上了,既然加上了就需要发挥它的作用,一般来说,check态的最大作用就是检测错误是否清除
当前是否达到并网条件(如电压电流是否符合...),然后进入并网态,我目前为止没看到你有执行继电器自检,母线检查,初始化变量,清除中断标志等操作,
原代码是有一套完整的流程的.

3. 通过学习老代码,可以确定倒计时和计数判断逻辑就应该在下面两个函数里面:State_SourceReady()和State_GridReady()
    但是我看你的代码逻辑是:并网运行时,母线或者PV波动会进入故障态,老代码进入的是等待态,这确实是更保险了,但是会不会导致重新并网时间较长?

4.老代码有硬件打嗝保护机制,现在我们还没有啊

5.原代码是有一套倒计时逻辑的,我们还没有啊

6.原代码打嗝保护标志位置位不仅仅是在TZ中断函数里面,还在ADC采样里面,检测到过流也会软件置位

7.原代码在ADC中断里面进行数据搬运和数据累加,检测到过零点信号之后,置位过零点标志位,然后给出信号量,执行GridPeriodCalc任务,在那里进行数据计算
我现在真的有点看不懂我们目前ADC中断函数了,能解释一下吗,

8.我发现我们的数据域居然没有PV功率,Grid功率,这不太合理吧

9.我发现老代码还有一个很好的思路,就是错峰调度:它在电网峰值的时候,释放了DC控制的信号量,这样就和过零点错开了,我们是不是也可以学习一下?
schedule正好还是空的,是时候发挥它的功能了
而且电网存在性也是在ADC中断里面判断的,我还发现void State_EnterSafeOutput(void)这个函数里面的操作在各个函数里面都会经常用到,是不是可以考虑移植到
system.c里面

10.我看老代码进行无功功率矫正的时候,算好了相位角,然后在此基础上加上△θ,我们该怎么做呢
 
11.pll结构体里面可以暂时不要最终的sin和cos结果,因为我发现那没有意义,可以放到电流环里面现场算,这样可以直接用无功补偿后的结果,
而无功补偿算的的θ就可以叠加到PLL算出的θ上了.

12.原代码是有较为完善的电流环初始化流程的,我们这个代码初始化有点简单了

13.原代码是直接使用ADC原始值进行电流环的,我觉得这个我们可以采纳,
我决定先复用原代码,先跑通
当然实际的参数我也算出来了,具体是Kp=9.09,Ki=0.909,前馈系数=0.8






我现在对当前的数据管理其实有点不太满意了
其实还是命名的问题
我有个改进建议,是什么呢:

typedef struct
{
    Uint16 gridVoltage;
    Uint16 inductorCurrent;
    Uint16 gfciCurrent;
    Uint16 dcBusVoltage;
    Uint16 gridDcCurrent;
    Uint16 inverterVoltage;
    Uint16 pv1Current;
    Uint16 pv2Current;
    Uint16 pv1Voltage;
    Uint16 pv2Voltage;
    Uint16 pv1Isolation;
    Uint16 pv2Isolation;
    Uint16 inverterTemperature;
    Uint16 boostTemperature;
} ADC_UintData;

typedef struct
{
    Float gridVoltage;
    Float inductorCurrent;
    Float gfciCurrent;
    Float dcBusVoltage;
    Float gridDcCurrent;
    Float inverterVoltage;
    Float pv1Current;
    Float pv2Current;
    Float pv1Voltage;
    Float pv2Voltage;
    Float pv1Isolation;
    Float pv2Isolation;
    Float inverterTemperature;
    Float boostTemperature;
} ADC_FloatData;

typedef struct
{

    ADC_UintData    rawInstant;
    ADC_FloatData   realInstant
    ADC_FloatData   realAvg;
    ADC_FloatData   realRms;

    PowerData       powerData;

    Uint32 measureSeq;
    Uint16 ecapFreqCent;
    Uint16 pllFreqCent;
    ...
    ...
} MachineData;

你看,ADC采集到的值只有两类:
未经处理的:ADC_UintData;
经过处理的:ADC_FloatData;
你可能会想:    
inverterTemperature和boostTemperature这两个数据根本没必要去记录原始值,瞬时真实值...
而ADC_UintData和ADC_FloatData里面却有它们,没意义啊
确实是这样的,但是我们虽然初始化了它,却没必要赋值他,也没必要读取它,不是吗?


那这样说的话,类似于ADC_FastRawFrame这样的结构体岂不是没意义了?



