#include "F28x_Project.h"

#include "power_limit.h"
#include "variable.h"

void PowerLimit_Init(void)
{
    /* Framework only: keep the existing default limit until the manager is implemented. */
}

void PowerLimit_Update(void)
{
    /* Framework only: thermal, PV, frequency and user limits are not active yet. */
}
